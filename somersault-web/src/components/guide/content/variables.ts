export default `
// Variables store values. To declare a varible use one of the pre-defined types:

int
float
string

// Variable names must be valid identifiers: start with a letter, continue with letters, digits or an underscore (_). Variable names are case-insensitive:

int count
float p
string model

// Multiple variables of the same type can be declared in one line with each name separated with the comma:

float x, y, z

// Variables can be assigned an initial value that must be of the same type:

int size = 50
float height = 46.5
string name = "CJ"

// Multiple variables can be assigned initial values too:

float x1, y1, z1 = -120.0, 500.5, 12.1

// Variables can be reassigned to different values or variables of the same type:

int min = 50
int max = 100

min = 40
min = max
`;
